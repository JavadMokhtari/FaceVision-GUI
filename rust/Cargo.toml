[package]
name = "rust_lib_facevision_gui"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
facevision-parallel = { path = "../../facevision-workspace/crates/facevision-parallel" }
flutter_rust_bridge = "=2.12.0"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(frb_expand)'] }
